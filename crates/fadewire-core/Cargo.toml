[package]
name = "fadewire-core"
description = "Platform-neutral fader logic for FadeWire: calibration tapers, signal filtering, mute detent, and the config model"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
toml = "0.8"
